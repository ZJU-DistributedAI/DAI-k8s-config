kind: Service
apiVersion: v1
metadata:
  name: ipfs-service
spec:
  type: NodePort
  selector:
    app: ipfs
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30001
    - protocol: TCP
      port: 8080
      targetPort: 30002